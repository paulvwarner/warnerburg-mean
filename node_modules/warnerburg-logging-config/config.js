var log = require('loglevel');

module.exports = function () {
    if (process.env.NODE_ENV == 'development') {
        log.setLevel("debug");
    } else {
        log.setLevel("warn");
    }

    return log;
};